const Users = require('../connections/users')
const mongoose = require('mongoose') // required because maybe it gives access to mongoose shit?


// exports.getAllGames = async (req, res) => { // GETS ALL GAMES

//     const games = await Games.find({})
//     // res.status(200).json(games);
//     res.send(games)

// }
